{
  "name": "chat-rag-tui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "tui": "tsx tui/src/index.ts",
    "typecheck": "tsc --noEmit -p tui/tsconfig.json",
    "test": "vitest run",
    "build": "tsc -p tui/tsconfig.build.json"
  },
  "dependencies": {
    "@earendil-works/pi-tui": "0.80.7",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}

